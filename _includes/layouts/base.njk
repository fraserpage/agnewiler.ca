<!doctype html>
<html {% if section %} data-current="{{ section }}"{% endif %} data-url="{{page.url}}" lang="en">

	{% include "components/head.njk" %}

	<body class="mobile-nav">

		<header id="header" class="{% if page.url !== "/" %} show-header-always {% endif %}" >
			<div class="content">
				<div class="logo">
					<a href="{{ '/' | url }}">{{ settings.title | safe }}</a>
				</div>
				{% include "components/nav.njk" %}
			</div>
		</header>

		<main>
			{{ layoutContent | safe }}
		</main>

		{% include "components/footer.njk" %}

	</body>

</html>
